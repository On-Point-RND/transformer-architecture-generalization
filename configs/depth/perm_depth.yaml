
include: base.yaml

paths:
  run_dir: runs/depth/perm_depth

model:
  n_layer: [1, 2, 4, 8]
  block_size: 16  

task:
  name: [s5, c5]
  params: {n_perms: 8}
  n_train: 1000000

train:
  seed: [0, 1, 2, 3, 4]
  max_iters: 60000

optimizer:
  lr_decay_iters: 60000
