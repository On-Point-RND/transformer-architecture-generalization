# Axis 6: positional encoding. The five schemes of the source branch; the file
# supports more (fope, relative_bias, cope, cape) and compositions with '+'.
include: base.yaml

model:
  name: positional
  pos_encoding: [nope, wpe, sinusoidal, rope, alibi]
